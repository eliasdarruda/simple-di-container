import { Container, Injectable, Injection, AliasInjection } from './src/container';
import { injectable, dep } from './src/dependency-decorator';

export { Container, Injectable, Injection, AliasInjection, injectable, dep };